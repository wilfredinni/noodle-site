<nav id="site-nav" aria-label="Primary navigation">
  <div class="nav-inner">
    <a href="/" class="wordmark" aria-label="Noodle home">
      <span>NOODLE</span><span class="prompt" aria-hidden="true">&gt;_</span>
    </a>

    <div class="nav-links" id="primary-navigation">
      <a href="/docs/">Docs</a>
      <a href="/blog/">Blog</a>
      <a href="https://github.com/wilfredinni/noodle" target="_blank" rel="noopener noreferrer">Source</a>
      <a href="https://github.com/wilfredinni/noodle/releases" target="_blank" rel="noopener noreferrer">Releases</a>
      <a class="install-link" href="/docs/getting-started/installation/">Get Noodle</a>
    </div>

    <button class="menu-toggle" id="menu-toggle" aria-label="Open navigation menu" aria-controls="primary-navigation" aria-expanded="false">
      <span></span><span></span>
    </button>
  </div>
</nav>

<style>
  #site-nav {
    position: sticky;
    top: 0;
    z-index: 20;
    width: 100%;
    min-height: 58px;
    background: color-mix(in srgb, var(--bg) 94%, transparent);
    border-bottom: 1px solid var(--rule);
    backdrop-filter: blur(12px);
  }

  .nav-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: min(100%, 1280px);
    min-height: 58px;
    margin-inline: auto;
    padding-inline: 1.5rem;
  }

  .wordmark {
    display: inline-flex;
    align-items: center;
    gap: 0.7rem;
    color: var(--fg);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.22em;
    text-decoration: none;
  }

  .prompt {
    color: var(--cyan);
    letter-spacing: -0.12em;
  }

  .nav-links {
    display: flex;
    align-items: center;
    gap: 1.8rem;
  }

  .nav-links a {
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.7rem;
    letter-spacing: 0.04em;
    text-decoration: none;
    transition: color 180ms ease;
  }

  .nav-links a:hover {
    color: var(--fg);
  }

  .nav-links .install-link {
    display: inline-flex;
    align-items: center;
    min-height: 32px;
    padding-inline: 0.8rem;
    color: var(--fg);
    background: var(--violet);
    border: 1px solid var(--violet);
    border-radius: 6px;
  }

  .nav-links .install-link:hover {
    color: var(--fg);
    background: var(--accent-dim);
    border-color: var(--accent-dim);
  }

  .menu-toggle {
    display: none;
    width: 44px;
    height: 44px;
    padding: 0;
    background: transparent;
    border: 0;
    cursor: pointer;
  }

  .menu-toggle span {
    display: block;
    width: 22px;
    height: 1px;
    margin: 6px auto;
    background: var(--fg);
    transition: transform 180ms ease;
  }

  @media (max-width: 760px) {
    .nav-inner {
      min-height: 64px;
      padding-inline: 1rem;
    }

    .nav-links {
      position: absolute;
      inset: 64px 0 auto;
      display: none;
      align-items: stretch;
      padding: 0.8rem 1rem 1rem;
      background: var(--bg);
      border-bottom: 1px solid var(--border);
    }

    #site-nav.menu-open .nav-links {
      display: grid;
    }

    .nav-links a {
      display: flex;
      align-items: center;
      min-height: 44px;
      border-bottom: 1px solid var(--rule);
    }

    .nav-links .install-link {
      justify-content: center;
      margin-top: 0.5rem;
      border: 1px solid var(--border);
    }

    .menu-toggle {
      display: block;
    }

    #site-nav.menu-open .menu-toggle span:first-child {
      transform: translateY(3.5px) rotate(45deg);
    }

    #site-nav.menu-open .menu-toggle span:last-child {
      transform: translateY(-3.5px) rotate(-45deg);
    }
  }
</style>

<script>
  const toggle = document.getElementById("menu-toggle")
  const nav = document.getElementById("site-nav")

  if (toggle && nav) {
    const close = () => {
      nav.classList.remove("menu-open")
      toggle.setAttribute("aria-expanded", "false")
      toggle.setAttribute("aria-label", "Open navigation menu")
    }

    toggle.addEventListener("click", () => {
      const isOpen = nav.classList.toggle("menu-open")
      toggle.setAttribute("aria-expanded", String(isOpen))
      toggle.setAttribute("aria-label", isOpen ? "Close navigation menu" : "Open navigation menu")
    })
    document.addEventListener("keydown", (event) => event.key === "Escape" && close())
    nav.querySelectorAll("a").forEach((link) => link.addEventListener("click", close))
  }
</script>
