---
title: Collection Export
description: Export Noodle collections as OpenAPI or Postman files
---

## OpenAPI

```bash
noodle export ./collections --format openapi --output ./specs/openapi.yml
```

The document includes enabled request parameters and headers, request-body
examples, folders as tags, supported authentication schemes, and each enabled,
nonempty environment `base_url` as a server. Other environment values and
response timeline data are not exported.

NTLM and AWS SigV4 export as HTTP security schemes. Validate those schemes with
the OpenAPI consumer that will read the document.

## Postman

```bash
noodle export ./collections --format postman --output ./exports/postman
```

The output directory must be new or empty. Noodle writes
`collection.postman_collection.json` and one redacted
`<environment>.postman_environment.json` file per environment. Request literals
remain intact and can contain secrets, so review a bundle before sharing it.

Home-relative `@/` file paths expand to absolute paths in Postman output. That
keeps file requests runnable, but can reveal local usernames and directories.

Noodle-specific TLS settings and OS-vault credentials are not translated into
OpenAPI or Postman output.

Postman output uses its matching `ntlm` and `awsv4` auth representations. On
import, Noodle maps NTLM from OpenAPI, Postman, and Insomnia sources, and maps
AWS SigV4 from Postman. Review imported credentials and move sensitive values
to secret environment declarations before sending.

For either format, use an output path outside the collection directory.

## Export in the TUI

Open the command palette with `Ctrl+P` and choose **Export Collection**.
Select OpenAPI or Postman and an output folder; the overlay previews the exact
target before exporting. If the default Postman target already exists, Noodle
uses the next numbered directory.
